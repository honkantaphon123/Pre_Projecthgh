<% for(var i = 0; i < order.length; i++) { %>
    <tr>

        <td><%= i + 1 %></td>
        <td><%= order[0].orderlist %></td>
        <td><%= order[i].qty %></td> 
            <input type="submit" value="Confirm Order" class="btn btn-success"/>
            <input type="hidden" name="id" value="<%= order[i]._id %>">

        
    </tr>

    <% } %>